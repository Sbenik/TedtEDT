
&НаСервере
Процедура ДанныеСправочникаПриИзмененииНаСервере()
	
	Объект.Итого = 0;
	Для каждого СтрокаДанных Из Объект.ДанныеСправочника Цикл
		Объект.Итого = Объект.Итого + СтрокаДанных.Нормочасы
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ДанныеСправочникаПриИзменении(Элемент)
	ДанныеСправочникаПриИзмененииНаСервере();
КонецПроцедуры
